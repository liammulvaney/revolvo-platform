# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @revolvo-backend/auth-api`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t revolvo-backend-auth-api`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system revolvo-backend-auth-api && \
          adduser --system -G revolvo-backend-auth-api revolvo-backend-auth-api

COPY apps/auth-api/dist revolvo-backend-auth-api/
COPY apps/auth-api/package.json revolvo-backend-auth-api/
RUN chown -R revolvo-backend-auth-api:revolvo-backend-auth-api .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix revolvo-backend-auth-api --omit=dev -f install

CMD [ "node", "revolvo-backend-auth-api" ]
